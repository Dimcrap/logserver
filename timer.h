#include <chrono>
#include <functional>



class timer {
    struct  scheduledTask
    {
        std::chrono::steady_clock::time_point next_excution;
        std::chrono::seconds;

    };
    

    public:
    private:
}